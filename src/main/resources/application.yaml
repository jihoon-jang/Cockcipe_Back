spring:
  application:
    name: cockcipe
  datasource:
    # TODO DB 설정 변경
    url: jdbc:mysql://localhost:3306/cockcipe
    username: root
    password: 1q2w3e4r!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
